# Sample Document

This is a sample markdown document.

The content of markdown files in the `bipartite/` directory will be displayed in the main content area of the `index.html` page.

You can add more markdown files and update the `markdownFiles` array in `bipartite/index.html` to see them in the list.
